upstream {{ item.value.name }} {
        {{ item.value.lb_method }};
	zone {{ item.value.name }} {{ item.value.zone_size }};

	include /etc/nginx/conf.d/backend.d/servers/server_*.conf;

{% if sticky_cookie is defined %}
	sticky cookie srv_id expires=1h  path=/;
{% endif %}
}
